# Webhook Configuration
# Set to 'true' to enable webhooks in development/staging
WEBHOOKS_ENABLED=true

# Set to 'production' for production environment
NODE_ENV=production

# Optional: Add API key if Community Jameel API requires authentication
# WEBHOOK_API_KEY=your_api_key_here

# Optional: Custom webhook timeout (default: 10000ms)
# WEBHOOK_TIMEOUT=15000

# Optional: Custom webhook retries (default: 3)
# WEBHOOK_RETRIES=5
